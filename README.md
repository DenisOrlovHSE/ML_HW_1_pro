# Домашнее задание №1 Pro

## Этапы работы

### 1. Разведочный анализ данных (EDA)

**Что было сделано:**
- Загрузка датасета и предварительный анализ распределений признаков;
- Устранение пропусков и дубликатов;
- Парсинг числовых признаков с учетом единиц измерения;
- Визуализация распределений признаков;
- Построение матриц корреляций разных типов;
- Анализ распределения целевой переменной;
- Исследование некоторых признаков на выбросы.

**Выводы:**
- Распределение цены смещено вправо;
- Сильная положительная корреляция между `max_power`, `torque` и целевой переменной;
- Категориальные признаки `owner`, `transmission`, `seller_type` влияют на цену;
- `km_driven` имеет отличное от нормального распределение, может поотребоваться дополнительная обработка.

### 2. Модель только на вещественных признаках

**Что было сделано:**
- Обучение классической линейной регрессии;
- Имплементация $R^2$ и $adjusted R^2$ метрик;
- Применение различных типов регуляризаций ($L_0$, $L_1$, ElasticNet);
- Подбор гиперпараметров через GridSearchCV.

**Выводы:**
- Наилучший результат на данном этапе $R^2 = 0.601$;
- Ни одна из регуляризаций не занулила ни один из признаков, а также не улучшила результат.

### 3. Модель с добавлением категориальных признаков

**Что было сделано:**
- Применение target encoding к полю `name`;
- Применение one-hot encoding к остальным категориальным признакам;
- Применение $L_2$ регуляризации и GridSearchCV.

**Выводы:**
- Наилучший результат на данном этапе $R^2 = 0.879$;
- $L_2$ регуляризация в совокупности с категориальными признаками значительно улучшила результат.

### 4. Feature Engineering и другие улучшения

**Что было сделано:**
- Добавление нового признака `company`;
- Логарифмирование признака `km_driven` для улучшения распределения;
- Применение полиномиальных признаков;
- Применение $L_1$, $L_2$ и ElasticNet регуляризаций вместе с GridSearchCV.

**Выводы:**
- Наилучший результат $R^2 = 0.896$;
- $L_2$ и ElasticNet регуляризации показали одинаковый $R^2$ результат, но у ElasticNet был немного меньше MSE, поэтому данная модель оказалась лучшей;
- С учетом почти одинаковых результатов для разных регуляризаций, можно заключить, что основной прирост качесвта обеспечили полиномиальные фичи, а также `company` + обработанный `km_driven`.

**Архитектура лучшей модели:**
```
Pipeline([
    ('poly', PolynomialFeatures(degree=2, include_bias=False)),
    ('scaler', StandardScaler()),
    ('elasticnet', ElasticNet(alpha=0.1, l1_ratio=0.5))
])
```

**Параметры ElasticNet:**
- `alpha=0.1`: сила регуляризации
- `l1_ratio=0.5`: баланс между Ridge (L2) и Lasso (L1)

### 5. Бизнес метрики

**Что было сделано:**
- Имплиментирование указанной бизнес метрики;
- Разработка своей метрики с учетом смещения в сторону занижения предсказаний.

**Выводы:**
- Бизнес метрики подтвердили результаты $R^2$ и MSE: Модель, полученная на 4 этапе, оказалась лучшей.

### 6. Разработка веб-приложения

**Реализованные страницы:**

#### EDA
- Интерактивные графики распределения целевой переменной;
- Матрица корреляций с отображением значений в ячейках;
- Сетка scatter plots для анализа зависимостей;
- Визуализация эффекта логарифмического преобразования.

#### Inference
- CSV uploader для пакетной обработки данных;
- Форма ручного ввода характеристик автомобиля;
- Унифицированный интерфейс (CSV + ручной ввод одновременно);
- Отображение предсказанных цен с статистикой;
- Скачивание результатов в CSV.

#### Model Analysis
- Топ-20 наиболее важных признаков;
- Анализ разреженности модели (процент обнуленных коэффициентов);
- Распределение всех коэффициентов модели;
- Отдельное отображение факторов, увеличивающих/уменьшающих цену;
- Полная таблица всех коэффициентов для детального анализа.

---

## Результаты и метрики

### Метрики модели:
- **R² Score**: ~0.896 (на тестовом наборе)
- **Количество признаков после трансформации**: 560 (с полиномиальными комбинациями)
- **Разреженность**: ~21% коэффициентов обнулены благодаря L1 регуляризации
---

## Анализ сервиса

### Удобство использования

**Что удалось:**
- Интуитивный интерфейс с кастомной боковой панелью;
- Два способа ввода данных (CSV + форма) в одном месте;
- Четкое разделение функций по страницам (EDA, Inference, Analysis);
- Кэширование данных предотвращает пересчеты.

**Потенциальные улучшения:**
- Добавить валидацию входных данных с подробными сообщениями об ошибках;
- Показывать примеры корректного формата CSV файла;
- Сохранять историю предсказаний в сессии.

### Качество визуализаций

**Что удалось:**
- Интерактивные графики Plotly (hover, zoom, pan);
- Матрица корреляций с отображением значений в ячейках;
- Color-coded коэффициенты (зеленый = положительный, красный = отрицательный);
- Правильное масштабирование и размеры графиков;
- Кастомные статистические боксы на графиках.

**Менее удачные решения:**
- Сетка scatter plots на EDA иногда становится переполненной;
- Матрица корреляций может быть тяжелой для браузера на больших наборах;
- Названия признаков после PolynomialFeatures очень длинные.

### Выявленные проблемы

- CSV с большим количеством строк может быть медленным;
- На UI появляются артефакты, пока прогружаются страницы;
- Несмотря на то что train датасет кэшируется, его все равно требуется подрузить отдельно для каждой страницы. 

---

## Планы на следующую итерацию

- Добавить возможность загружать не только тестовые данные, но и ответы к ним, чтобы сравнить эффективность модели;
- Оптимизировать подгрузку ресурсов и UI;
- Улучшить качество предсказаний модели.

## Заключение 📝

Разработанное приложение успешно демонстрирует полный цикл ML-проекта: от разведочного анализа данных через построение оптимальной модели (R² = 0.896 с ElasticNet + полиномиальными признаками) к production-ready веб-сервису на Streamlit. Основной вклад в качество дали полиномиальные признаки, целевое кодирование и ElasticNet регуляризация.